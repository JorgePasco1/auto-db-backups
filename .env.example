# Cloudflare R2 Configuration (required)
# --------------------------------------
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key
R2_SECRET_ACCESS_KEY=your-r2-secret-key
R2_BUCKET_NAME=your-bucket-name

# Multiple Database Connections
# -----------------------------
# Format: DATABASE_CONNECTION_N where N is 1, 2, 3, etc.
# The system will automatically detect all numbered connections.
# IMPORTANT: Wrap connection strings in single quotes if they contain & or other special characters
DATABASE_CONNECTION_1='postgresql://user:password@host1:5432/database1?sslmode=require'
DATABASE_CONNECTION_2='postgresql://user:password@host2:5432/database2?sslmode=require'
DATABASE_CONNECTION_3='postgresql://user:password@host3:5432/database3?sslmode=require'

# Optional: Custom database name for backup files (defaults to name from connection string)
# Use this to give meaningful names instead of generic "neondb" from connection strings
# DATABASE_NAME_1=users-db
# DATABASE_NAME_2=orders-db

# Optional: Per-database backup prefix (defaults to backups/<dbname>/)
# DATABASE_PREFIX_1=backups/mydb1/
# DATABASE_PREFIX_2=backups/mydb2/

# Database type (default: postgres)
# Supported: postgres, mysql, mongodb
# DATABASE_TYPE=postgres

# Backup Options
# --------------
# Compression (default: true)
# COMPRESSION=true

# Encryption key (optional, must be 32-byte base64-encoded)
# Generate with: openssl rand -base64 32
# ENCRYPTION_KEY=your-base64-encoded-32-byte-key

# Retention Policy (optional)
# ---------------------------
# Delete backups older than N days (0 = disabled)
# RETENTION_DAYS=30
# Keep only the last N backups per database (0 = disabled)
# RETENTION_COUNT=10

# Notifications (optional)
# ------------------------
# WEBHOOK_URL=https://hooks.slack.com/services/...
# NOTIFY_ON_SUCCESS=true
# NOTIFY_ON_FAILURE=true
