# Cloudflare R2 Configuration (required)
# --------------------------------------
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key
R2_SECRET_ACCESS_KEY=your-r2-secret-key
R2_BUCKET_NAME=your-bucket-name

# Database Configuration (required)
# ---------------------------------
# DATABASES_JSON is a JSON array of database objects. Each object has:
#   - connection (required): Full connection string URL
#   - name (optional): Custom name for backup files (defaults to parsing from connection string)
#   - prefix (optional): Custom backup prefix (defaults to backups/{name}/)
#   - type (optional): Database type override (postgres, mysql, mongodb) - defaults to DATABASE_TYPE
#
# Example with single database:
# DATABASES_JSON='[{"connection": "postgresql://user:password@host:5432/mydb"}]'
#
# Example with multiple databases:
# DATABASES_JSON='[
#   {"connection": "postgresql://user:password@host1:5432/users", "name": "users-db"},
#   {"connection": "postgresql://user:password@host2:5432/orders", "name": "orders-db", "prefix": "prod/orders/"},
#   {"connection": "mysql://user:password@host3:3306/analytics", "type": "mysql"}
# ]'
#
# IMPORTANT: Wrap the entire value in single quotes to preserve JSON formatting
DATABASES_JSON='[{"connection": "postgresql://user:password@host:5432/database?sslmode=require"}]'

# Global database type (default: postgres)
# Used when individual databases don't specify a type
# Supported: postgres, mysql, mongodb
# DATABASE_TYPE=postgres

# Backup Options
# --------------
# Compression (default: true)
# COMPRESSION=true

# Encryption key (optional, must be 32-byte base64-encoded)
# Generate with: openssl rand -base64 32
# ENCRYPTION_KEY=your-base64-encoded-32-byte-key

# Retention Policy (optional)
# ---------------------------
# Delete backups older than N days (0 = disabled)
# RETENTION_DAYS=30
# Keep only the last N backups per database (0 = disabled)
# RETENTION_COUNT=10

# Notifications (optional)
# ------------------------
# WEBHOOK_URL=https://hooks.slack.com/services/...
# NOTIFY_ON_SUCCESS=true
# NOTIFY_ON_FAILURE=true
